package linkedlist

// /*

// 	定义两个辅助指针p1，p2,这两个指针均往next方向移动，移动的过程中保持p1之前的值都小于选定的pivot，
// 	p1和p2之间的值都大于选定的pivot，那么当p2走到末尾时交换p1的值与pivot便完成了一次切分
// 	p1 指向的永远是最后一个小于 pivot的元素
// */

// 输入: -1->5->3->4->0
// 输出: -1->0->3->4->5

func sortList12(head *ListNode) *ListNode {
	return head // 返回排序后的链表
}
